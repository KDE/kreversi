########### install files ###############

install( FILES
	# default_theme.desktop is not installed at the moment because it's not necessary
	default_theme.svgz
DESTINATION  ${DATA_INSTALL_DIR}/kreversi/pics )
